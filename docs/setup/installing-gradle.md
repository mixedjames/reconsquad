# Installing Gradle

Since we did this on Mac I used Homebrew:
`brew install gradle`

This should be something we would generally only need to do once because the
`gradlew` script should handle getting it for new devs.